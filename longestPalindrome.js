function longestPalindrome(str) {

   
    let left = 0
    for (let i = 0; i < str.length;) {
        str
        
    }

    console.log(curr);
    console.log(longest);

}

// const str = "babad"
const str = "cbbd"

const res = longestPalindrome(str)

console.log(res);
